node_modules/
.bun.lock
*.jsonc
*.json.bak
*.log
.DS_Store
opencode-notifier-state.json

# BEGIN opencode feature surfaces
# Root feature files are local state for this config repo — not tracked here.
# Downstream project repos initialized from project/ may track feature markdown;
# the init command recommends adding exceptions there:
#   !features/
#   !features/*.md
features/
# END opencode feature surfaces
